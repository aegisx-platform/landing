# @aegisx/landing

AegisX Platform landing page. Extracted from [aegisx-starter](https://github.com/aegisx-platform/aegisx-starter) monorepo.

## Setup

This app was an Nx project inside the monorepo. For standalone use you will need to add a `package.json`, `tsconfig.json`, and build tooling.
